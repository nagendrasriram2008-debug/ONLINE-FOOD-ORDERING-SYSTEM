# ONLINE-FOOD-ORDERING-SYSTEM
This C++ program is a lightweight command-line food ordering system. It uses a basic while loop to display a menu of Burgers, Pizzas, and Salads. Users type a number to select their item and specify the quantity. The program calculates costs using if-else blocks, adds it to a running total, and then prints a final receipt at checkout
